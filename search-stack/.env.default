# Public Instance URL
SEARXNG_BASE_URL=http://your-server:9704/

# Secret key — change it with a random value
# e.g. generate it with  openssl rand -hex 32
SEARXNG_SECRET_KEY=CHANGE_ME_openssl_rand_hex_32
